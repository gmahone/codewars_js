numToStringConvert = function(num){
    return(String(num));
}

/* add other solutions */

function numberToString(num) {
  return num.toString();
}

function numberToString(num) {
  // Return a string of the number here!
  return String(num);
}
